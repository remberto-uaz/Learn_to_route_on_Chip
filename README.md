# Reproducibility Package —  Scoping Learning to Route On-Chip: A Survey of Machine Learning-Based Routing in Networks-on-Chip and Its Hardware Overheads

This package reconstructs the entire systematic review.

## How to reproduce
1. Read `protocol/protocol.md` (version v2.0, status Approved).
2. Re-run the strategies in `searches/searches.json` on the listed dates/filters.
3. Import `references/references.bib` (175 records, 0 marked duplicateOf).
4. Verify screening decisions in `screening/screening.csv` (per reviewer, with reasons).
5. Check quality in `quality/quality.csv` and extraction in `extraction/extraction.csv`.
6. Cross-check the PRISMA flow in `prisma/flow.json` — all identities hold.
7. Every state change is in `audit/audit.json` (1220 events, append-only).

## Integrity
`manifest.json` lists a SHA-style hash per file. The package was generated 2026-09-10T14:54:27.749Z by SR Research Workspace.
